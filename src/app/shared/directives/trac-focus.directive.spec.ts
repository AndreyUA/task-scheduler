import { TracFocusDirective } from './trac-focus.directive';

describe('TracFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new TracFocusDirective();
    expect(directive).toBeTruthy();
  });
});
